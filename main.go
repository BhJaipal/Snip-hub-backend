package main

func main() {
	// ctx, cancel := context.WithTimeout(context.Background(), 1 * time.Microsecond)
	// defer cancel()
	// client, err := mongo.Connect(ctx, options.Client().ApplyURI("mongodb://localhost:27017"))
	// if err != nil {
		// log.Fatal(err)
	// }
	// langSnips := []Language{};
	// snipHub := client.Database("snip-hub")
	// resolver := resolvers.SniphubDbResolvers{Db: snipHub, Ctx: ctx}
	// print(resolver)
	// input, err := os.ReadFile("./schema.gql")
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// source := ast.Source{Name: "schema.gql", Input: string(input), BuiltIn: false}
	// schema := gqlparser.MustLoadSchema(&source)
}